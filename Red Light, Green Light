# Red Light, Green Light

cases = int(input())
for i in range(cases):
    n,k = map(int,input().split())
    lst = list(map(int,input().split()))[:n]
    count=0
    for j in range(n):
        if lst[j]>k:
            count += 1
    print(count)
