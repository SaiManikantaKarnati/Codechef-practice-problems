# Passing Marks

# cook your dish here
cases = int(input())
for _ in range(cases):
    n,x = map(int,input().split())
    arr = list(map(int,input().split()))
    arr.sort(reverse = True)
    print(arr[x-1]-1)
    
