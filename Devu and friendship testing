# Devu and friendship testing

# cook your dish here
cases = int(input())
for _ in range(cases):
    n = int(input())
    lst = list(map(int,input().split()))[:n]
    result = set(lst)
    print(len(result))
