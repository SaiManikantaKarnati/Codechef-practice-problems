# Zero String

# cook your dish here
cases = int(input())
for _ in range(cases):
    n = int(input())
    s = input()
    zeros = s.count("0")
    ones = s.count("1")
    if zeros >= ones:
        print(ones)
    else:
        print(zeros+1)
